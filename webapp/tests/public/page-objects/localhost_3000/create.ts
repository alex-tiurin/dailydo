import { CreatePageBase } from '../../../page-objects/localhost_3000/create.generated'

export class CreatePage extends CreatePageBase {
  // Add custom interaction methods here, for example:
  //
  // async login(username: string, password: string) {
  //   await this.usernameInput.fill(username)
  //   await this.passwordInput.fill(password)
  //   await this.submitButton.click()
  // }
}
